export enum RoleName {
  SYSTEMADMIN = "system admin",
  ADMIN = "admin",
  EMPLOYEE = "employee",
  CUSTOMER = "customer",
}

export enum TokenTableName {
  EMAIL = 'email_verification_token',
  PASSWORD = 'reset_password_token',
}